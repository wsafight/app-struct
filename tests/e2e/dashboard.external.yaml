version: 1

app:
  name: m5-browser

database:
  provider: postgres
  dev:
    mode: external

modules:
  auth:
    enabled: true
    user_entity: User
    registration: true
    password_reset: true
  rbac:
    roles: [member, admin]
    default_role: member

includes:
  - spec/identity.yaml
  - spec/project.yaml
